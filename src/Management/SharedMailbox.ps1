# SharedMailbox.ps1 - Shared Mailbox view is embedded in MainWindow.ps1

function Show-SharedMailboxWindow {
    param([System.Windows.Window]$Owner)
    # View is now embedded in the main window - this function is not used
}
